#define pi 3.14
#include<stdio.h>
void main()
{
    int area,radius;
    printf("enter the radius: ");
    scanf("%d",&radius);

    area=2*pi*radius;

    printf("the area os circle is: %d",area);
}
